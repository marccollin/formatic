/*
package com.formatic.dto;

import com.formatic.form.FormField;
import com.formatic.form.FormFieldType;

public class ExtendedTestDto {

    @FormField(type = FormFieldType.TEXTAREA, rows = 5, cols = 50, order = 3)
    private String description;

    @FormField(type = FormFieldType.TEXT,
            minLength = 3, maxLength = 50,
            pattern = "^[A-Za-z]+$",
            order = 4)
    private String code;

}
*/
