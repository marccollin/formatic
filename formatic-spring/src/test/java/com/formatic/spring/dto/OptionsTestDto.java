/*
package com.formatic.dto;

import com.formatic.form.FormField;
import com.formatic.form.FormFieldType;

public class OptionsTestDto {
    @FormField(type = FormFieldType.SELECT,
            options = {"FR:France", "US:États-Unis", "CA:Canada"},
            order = 1)
    private String country;

    @FormField(type = FormFieldType.RADIO,
            options = {"S", "M", "L", "XL"},
            order = 2)
    private String size;
}
*/
